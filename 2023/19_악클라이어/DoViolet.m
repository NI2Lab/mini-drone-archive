function activeend = DoViolet(img, center, coordinate)

DistanceGood = 0;
getCenter = 0;

while DistanceGood
    moveforward(dr, );
    DistanceGood = CheckDistance(img);
end

while getCenter
     getCenter= AimStraight(center, coordinate);
end

activeend = 1;
land(dr);
end